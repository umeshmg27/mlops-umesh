apiVersion: v1
kind: Service
metadata:
  name: heart-api
  namespace: heart-disease
spec:
  selector:
    app: heart-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
  type: LoadBalancer

